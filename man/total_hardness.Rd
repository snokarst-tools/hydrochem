% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/total_hardness.R
\name{total_hardness}
\alias{total_hardness}
\title{Calculate Total Hardness}
\usage{
total_hardness(
  data,
  base_unit = c("mg/L", "mmol/L", "meq/L"),
  output = c("mg/L_CaCO3", "fH", "dH", "e")
)
}
\arguments{
\item{data}{A \code{data.frame} or \code{data.table} containing the required ion
columns \code{"Ca"} and \code{"Mg"}.}

\item{base_unit}{Unit of the input ion concentrations. One of \code{"mg/L"},
\code{"mmol/L"}, or \code{"meq/L"}. Default is \code{"mg/L"}.}

\item{output}{Unit of the returned hardness.
One of:
\itemize{
\item \code{"mg/L_CaCO3"} — milligrams per liter as CaCO3 (default)
\item \code{"fH"} — French degrees
\item \code{"dH"} — German degrees
\item \code{"e"} — English degrees
}}
}
\value{
A numeric vector of Total Hardness values (in mg/L as CaCO3), one per
row of \code{data}.
}
\description{
Computes total water hardness from hydrochemical data using calcium and
magnesium concentrations. Input
concentrations may be provided in \code{"mg/L"}, \code{"mmol/L"}, or \code{"meq/L"} and are internally converted to
\code{"mg/L"} prior to calculation.
}
\details{
The function checks that both required ions (\code{Ca}, \code{Mg}) are present in the
input dataset and throws an error if either is missing.
}
\examples{
data("hc_data", package = "hydrochem")
total_hardness(hc_data, base_unit = "mg/L")

}
