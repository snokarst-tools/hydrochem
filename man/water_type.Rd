% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/water_type.R
\name{water_type}
\alias{water_type}
\title{Determine Water Type}
\usage{
water_type(
  data,
  base_unit = c("mg/L", "mmol/L", "meq/L"),
  format = c("long", "short"),
  threshold = 20
)
}
\arguments{
\item{data}{A \code{data.frame} or \code{data.table} with columns named after ions (e.g., \code{"Ca"}, \code{"Cl"}, \code{"SO4"}).}

\item{base_unit}{Unit of the input ion concentrations. One of \code{"mg/L"}, \code{"mmol/L"}, or \code{"meq/L"}. Default is \code{"mg/L"}.}

\item{format}{Output format: \code{"short"} for dominant ions only, \code{"long"} to include all ions above a threshold. Default is \code{"long"}.}

\item{threshold}{Numeric. Minimum percentage for an ion to be considered dominant in \code{"long"} format. Default is \code{20}.}
}
\value{
A character vector of water types sorted in decreasing order, one per row of \code{data}.
}
\description{
Classifies each sample in a hydrochemical dataset by water type based on the dominant cations and anions.
Input concentrations may be
provided in \code{"mg/L"}, \code{"mmol/L"}, or \code{"meq/L"} and are internally converted
to \code{"meq/L"} prior to calculation.
}
\details{
Two formats are available:
\itemize{
\item \code{"short"}: returns the single most dominant cation and anion (e.g., \code{"Ca-HCO3"}).
\item \code{"long"}: returns all ions above a specified threshold, joined by \code{"+"} (e.g., \code{"Ca+Mg-HCO3+SO4"}).
}
}
\examples{
data("hc_data", package = "hydrochem")
water_type(hc_data, base_unit = "mg/L", format = "short")
water_type(hc_data, base_unit = "mg/L", format = "long", threshold = 25)
}
