% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_stiff.R
\name{plot_stiff}
\alias{plot_stiff}
\title{Plot Stiff Diagrams from Hydrochemical Data}
\usage{
plot_stiff(
  data,
  base_unit = c("mg/L", "mmol/L", "meq/L"),
  include_NO3 = NULL,
  idcol = NULL,
  group = NULL,
  group_custom = FALSE,
  nrow = NULL,
  ncol = NULL,
  scales = c("fixed", "free_x"),
  facet_dir = c("v", "h"),
  xlab = "meq/L",
  base_size = 12,
  bold_label = FALSE,
  color = "black",
  fill = "grey40",
  axis_title_size = 14,
  label_border_color = "transparent",
  plot_title = NULL,
  legend.position = "bottom",
  legend.title = NULL,
  show_label = TRUE,
  bg_color = "#FFFFFF"
)
}
\arguments{
\item{data}{A \code{data.frame} or \code{data.table} with columns named after ions (e.g., \code{"Ca"}, \code{"Cl"}, \code{"SO4"}).}

\item{base_unit}{Unit of the input ion concentrations. One of \code{"mg/L"}, \code{"mmol/L"}, or \code{"meq/L"}. Default is \code{"mg/L"}.}

\item{include_NO3}{Character or \code{NULL}. If set to \code{"Cl"} or \code{"SO4"}, nitrate (\code{NO3}) is included in the Stiff diagram by being added to the specified anion. Default is \code{NULL}.}

\item{idcol}{Character or \code{NULL}. Column name used to identify and facet individual diagrams.
If not provided, a sequential ID is created. If provided, it is converted to a factor with levels
ordered by first appearance in the data, ensuring facet order matches data order. Default is \code{NULL}.}

\item{group}{Optional character string. Name of the column used for coloring or filling groups. Default is \code{NULL}.}

\item{group_custom}{Logical. If \code{TRUE}, enables manual aesthetics for each group defined in \code{group}. Requires vectors \code{color}, \code{fill} (each of length equal to the number of groups). Default is \code{FALSE}.}

\item{nrow, ncol}{Integers. Number of rows and columns for the facet layout. Default is \code{NULL} (automatic layout).}

\item{scales}{Character. Facet scaling behavior. Must be \code{"fixed"} or \code{"free_x"}. Default is \code{"fixed"}.}

\item{facet_dir}{Character. Facet layout direction. Either \code{"v"} for vertical or \code{"h"} for horizontal. Default is \code{"v"}.}

\item{xlab}{Character. Label for the x-axis. Set \code{NULL} for no label. Default is \code{"meq/L"}.}

\item{base_size}{Numeric. Base font size for the plot. Default is \code{12}.}

\item{bold_label}{Logical. If \code{TRUE}, displays y-axis group labels in bold. Default is \code{FALSE}.}

\item{color}{Character or vector. Outline color(s) for polygons. If \code{group_custom = TRUE}, must be a vector. Default is \code{"black"}.}

\item{fill}{Character or vector. Fill color(s) for polygons. If \code{group_custom = TRUE}, must be a vector. Default is \code{"grey40"}.}

\item{axis_title_size}{Numeric. Font size for facet strip labels. Default is \code{14}.}

\item{label_border_color}{Character. Border color of strip labels. Use \code{"transparent"} for no border. Default is \code{"transparent"}.}

\item{plot_title}{Optional character string. Title of the plot. Default is NULL (no title displayed).}

\item{legend.position}{Position of the legend. Accepts \code{"top"}, \code{"bottom"}, \code{"left"}, \code{"right"}, \code{"none"}, or a numeric vector of length 2 for relative coordinates, e.g., \code{c(1, 0.5)}. Default is \code{"bottom"}.}

\item{legend.title}{A character string for the title of the legend. If \code{NULL}, no legend title is displayed. Default is \code{NULL}.}

\item{show_label}{Logical. If \code{TRUE}, displays strip labels in each facet. Default is \code{TRUE}.}

\item{bg_color}{Character. Background color of the plot area and panel. Accepts valid color names or hex codes (e.g., "red", "#FFFFFF"). Default is "#FFFFFF".}
}
\value{
A \code{ggplot} object representing the Stiff diagram(s).
}
\description{
Generates Stiff diagrams from hydrochemical data.
Input concentrations may be
provided in \code{"mg/L"}, \code{"mmol/L"}, or \code{"meq/L"} and are internally converted
to \code{"meq/L"} prior to calculation.
Optionally groups data points and allows manual customization of plotting aesthetics.
}
\examples{
data("hc_data", package = "hydrochem")
hc_data <- hc_data[1:20, ] # keep first 20 rows

# Basic Stiff plot
plot_stiff(hc_data)

# Stiff plot with sample IDs
plot_stiff(hc_data, idcol = "id")

# Stiff plot for selected samples only
sel_data <- hc_data[hc_data$id \%in\% c("id_1", "id_5", "id_20"), ]
plot_stiff(sel_data)
plot_stiff(sel_data, group = "type") # with grouping

# Stiff plot with controlled facet order
sel_data <- hc_data[hc_data$id \%in\% c("id_1", "id_5", "id_20"), ]
sel_data <- sel_data[order(sel_data$type, decreasing = TRUE), ]
plot_stiff(sel_data, idcol = "id", group = "type")

# Stiff plot with group and custom fill
plot_stiff(
  hc_data,
  idcol = "id",
  group = "type",
  group_custom = TRUE,
  fill = c("lightblue", "orange"),
  color = c("red", "red")
)

}
