% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/replace_bdl.R
\name{replace_bdl}
\alias{replace_bdl}
\title{Replace Values Below Detection Limit (BDL)}
\usage{
replace_bdl(
  data,
  cols = NULL,
  method = c("DL/2", "DL/sqrt(2)", "DL", "0", "NA")
)
}
\arguments{
\item{data}{A \code{data.frame} or \code{data.table}.}

\item{cols}{A character vector of cols names to summarize. If \code{NULL}, columns are detected automatically.}

\item{method}{Character. Method used to replace values below detection limits. One of:
\itemize{
\item \code{"DL/2"} (default): replaces with half the detection limit,
\item \code{"DL/sqrt(2)"}: replaces with DL divided by \eqn{\sqrt{x}}2,
\item \code{"DL"}: replaces with the detection limit itself,
\item \code{"0"}: replaces with zero.
\item \code{"NA"}: replaces with NA.
}}
}
\value{
A \code{data.table} where BDL character values have been replaced by numeric values according to the selected method.
}
\description{
Detects and replaces values below detection limits (e.g., \code{"<0.01"}, \code{"< 0.005"}, \code{"< .5"}, \code{" < 0.1 "}) in a hydrochemical dataset.
Only columns including at least one BDL value are processed. Values are replaced with a numeric approximation based on the specified method.
}
\examples{
df <- data.frame(Ca = c(" <0.01", "5.2", "< 0.005"), Cl = c("3.1", "<0.02", "2.5"))
df <- replace_bdl(df, method = "DL/2")
print(df)

data <- replace_bdl(data_qc, method = "0")
print(data)
}
