% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_stabler.R
\name{plot_stabler}
\alias{plot_stabler}
\title{Plot Stabler Diagrams from Hydrochemical Data}
\usage{
plot_stabler(
  data,
  base_unit = c("mg/L", "mmol/L", "meq/L"),
  include_NO3 = FALSE,
  ratio = 10,
  idcol = NULL,
  nrow = NULL,
  ncol = NULL,
  facet_dir = c("v", "h"),
  xlab = "\%meq/L",
  base_size = 12,
  border_color = "transparent",
  border_width = 0.5,
  axis_title_size = 14,
  label_border_color = "transparent",
  plot_title = NULL,
  legend.position = "bottom",
  legend.title = NULL,
  show_label = TRUE,
  bg_color = "#FFFFFF"
)
}
\arguments{
\item{data}{A \code{data.frame} or \code{data.table} with columns named after ions (e.g., \code{"Ca"}, \code{"Cl"}, \code{"SO4"}).}

\item{base_unit}{Unit of the input ion concentrations. One of \code{"mg/L"}, \code{"mmol/L"}, or \code{"meq/L"}. Default is \code{"mg/L"}.}

\item{include_NO3}{Logical. If \code{TRUE}, nitrate (\code{NO3}) is included. The \code{NO3} column must be present in the input data. Default is \code{FALSE}.}

\item{ratio}{Numeric. Aspect ratio between x and y axes in the plot. Default is \code{10}.}

\item{idcol}{Character or \code{NULL}. Column name used to identify and facet individual diagrams. If \code{NULL}, synthetic sample IDs are generated. Default is \code{NULL}.}

\item{nrow, ncol}{Integers. Number of rows and columns for the facet layout. Default is \code{NULL} (automatic layout).}

\item{facet_dir}{Character. Facet layout direction. Either \code{"v"} for vertical or \code{"h"} for horizontal. Default is \code{"v"}.}

\item{xlab}{Character. Label for the x-axis. Set \code{NULL} for no axis Default is \code{"\%meq/L"}.}

\item{base_size}{Numeric. Base font size for plot text. Default is \code{12}.}

\item{border_color}{Color of the bar border. Default is \code{"transparent"}.}

\item{border_width}{Width of the bar border. Default is \code{0.5}.}

\item{axis_title_size}{Numeric. Font size for strip labels. Default is \code{14}.}

\item{label_border_color}{Character. Border color for facet strip labels. Use \code{"transparent"} for no border. Default is \code{"transparent"}.}

\item{plot_title}{Optional character string. Title of the plot. Default is NULL (no title displayed).}

\item{legend.position}{Position of the legend. Accepts \code{"top"}, \code{"bottom"}, \code{"left"}, \code{"right"}, \code{"none"}, or a numeric vector of length 2 for relative coordinates, e.g., \code{c(1, 0.5)}. Default is \code{"bottom"}.}

\item{legend.title}{A character string for the title of the legend. If \code{NULL}, no legend title is displayed. Default is \code{NULL}.}

\item{show_label}{Logical. If \code{TRUE}, displays strip labels. Default is \code{TRUE}.}

\item{bg_color}{Character. Background color of the plot area and panel. Accepts valid color names or hex codes (e.g., "red", "#FFFFFF"). Default is "#FFFFFF".}
}
\value{
A \code{ggplot} object representing the Stabler diagram(s).
}
\description{
Generates Stabler diagrams from hydrochemical data.
Input concentrations may be
provided in \code{"mg/L"}, \code{"mmol/L"}, or \code{"meq/L"} and are internally converted
to \code{"meq/L"} prior to calculation.
Optionally groups data points and allows manual customization of plotting aesthetics.
}
\examples{
data("hc_data", package = "hydrochem")
hc_data <- hc_data[1:20, ]
plot_stabler(hc_data, idcol = "id")

}
