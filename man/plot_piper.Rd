% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_piper.R
\name{plot_piper}
\alias{plot_piper}
\title{Plot Piper Diagram from Hydrochemical Data}
\usage{
plot_piper(
  data,
  base_unit = c("mg/L", "mmol/L", "meq/L"),
  include_NO3 = FALSE,
  group = NULL,
  group_custom = FALSE,
  grid_lines = TRUE,
  axis_title_size = 3.5,
  axis_tick_label_size = 3,
  bold_label = FALSE,
  shape = 21,
  color = "black",
  fill = "grey40",
  size = 2,
  plot_title = NULL,
  label = NULL,
  label_size = 3,
  label_nudge_x = 0,
  label_nudge_y = 0,
  legend.position = "bottom",
  legend.title = NULL,
  bg_color = "#FFFFFF"
)
}
\arguments{
\item{data}{A \code{data.frame} or \code{data.table} with columns named after ions (e.g., \code{"Ca"}, \code{"Cl"}, \code{"SO4"}).}

\item{base_unit}{Unit of the input ion concentrations. One of \code{"mg/L"}, \code{"mmol/L"}, or \code{"meq/L"}. Default is \code{"mg/L"}.}

\item{include_NO3}{Logical. If \code{TRUE}, nitrate (\code{NO3}) is included. The \code{NO3} column must be present in the input data. Default is \code{FALSE}.}

\item{group}{Optional character string. Name of the column in \code{data} used to group points in the Piper diagram. If \code{NULL}, all points are plotted with the same style. Default is \code{NULL}.}

\item{group_custom}{Logical. If \code{TRUE}, enables manual aesthetics for each group defined in \code{group}.
In this case, at least one of \code{color}, \code{fill}, \code{shape}, or \code{size} must be provided as a vector of length equal to the number of groups.
Any missing or incorrectly sized vectors will be automatically filled with default values (e.g., \code{"black"}, \code{"transparent"}, \code{21}, \code{2}).
If \code{FALSE} (default), all these aesthetics must be of length 1.}

\item{grid_lines}{Logical. Whether to include dashed grid lines for the ternary fields and diamond. Default is \code{TRUE}.}

\item{axis_title_size}{Numeric. Text size for the ion group labels. Default is 3.5.}

\item{axis_tick_label_size}{Numeric. Text size for the tick value labels. Default is 3.}

\item{bold_label}{Logical. If TRUE, renders the ion group labels in bold. Default is FALSE.}

\item{shape}{Integer or vector of integers. Shape(s) for plotting points. Must follow ggplot2 shape standards. Default is 21.}

\item{color}{Character. Outline color for the plotted points. Default is \code{"black"}.}

\item{fill}{Character. Fill color for the plotted points. Default is \code{"grey40"}.}

\item{size}{Numeric. Size of the plotted points. Default is 2.}

\item{plot_title}{Optional character string. Title of the plot. Default is NULL (no title displayed).}

\item{label}{Optional column name in \code{data} used to label points. Default is \code{NULL} (no labels).}

\item{label_size}{Numeric. Size of the label text. Default is \code{3}.}

\item{label_nudge_x}{Numeric. Horizontal offset of labels relative to points. Default is \code{0}.}

\item{label_nudge_y}{Numeric. Vertical offset of labels relative to points. Default is \code{0}.}

\item{legend.position}{Position of the legend. Accepts \code{"top"}, \code{"bottom"}, \code{"left"}, \code{"right"}, \code{"none"}, or a numeric vector of length 2 for relative coordinates, e.g., \code{c(1, 0.5)}. Default is \code{"bottom"}.}

\item{legend.title}{A character string for the title of the legend. If \code{NULL}, no legend title is displayed. Default is \code{NULL}.}

\item{bg_color}{Character. Background color of the plot area and panel. Accepts valid color names or hex codes (e.g., "red", "#FFFFFF"). Default is "#FFFFFF".}
}
\value{
A ggplot object displaying the Piper diagram.
}
\description{
Generates a Piper diagram from hydrochemical data.
Input concentrations may be
provided in \code{"mg/L"}, \code{"mmol/L"}, or \code{"meq/L"} and are internally converted
to \code{"meq/L"} prior to calculation.
If carbonate (\code{CO3}) is also provided, its contribution is added
to bicarbonate after conversion to milliequivalents (\code{meq/L}). If \code{CO3} is absent, alkalinity is considered to be represented solely by bicarbonate (\code{HCO3}).
Optionally groups data points and allows manual customization of plotting aesthetics.
}
\examples{
data("hc_data", package = "hydrochem")

# Basic Piper plot without grouping
plot_piper(hc_data)

# Piper plot with simple grouping
plot_piper(hc_data, group = "type")

# Piper plot with custom aesthetics per group
plot_piper(
  hc_data,
  group = "type",
  group_custom = TRUE,
  fill = c("steelblue", "tomato", "darkgreen"),
  color = c("black", "black", "black"),
  shape = c(21, 22, 24),
  size  = c(2.5, 3, 3.5)
)

}
