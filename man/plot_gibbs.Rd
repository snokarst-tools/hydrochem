% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_gibbs.R
\name{plot_gibbs}
\alias{plot_gibbs}
\title{Plot Gibbs Diagram from Hydrochemical Data}
\usage{
plot_gibbs(
  data,
  base_unit = c("mg/L", "mmol/L", "meq/L"),
  tds_method = c("major", "major_si", "all_ions"),
  show_guide = TRUE,
  show_text = FALSE,
  group = NULL,
  group_custom = FALSE,
  base_size = 12,
  shape = 21,
  color = "black",
  fill = "grey40",
  size = 2,
  plot_title = NULL,
  label = NULL,
  label_size = 3,
  label_nudge_x = 0,
  label_nudge_y = 0,
  legend.position = c("bottom", "right", "none"),
  legend.title = NULL,
  bg_color = "#FFFFFF"
)
}
\arguments{
\item{data}{A \code{data.frame} or \code{data.table} with columns named after ions (e.g., \code{"Ca"}, \code{"Cl"}, \code{"SO4"}).}

\item{base_unit}{Unit of the input ion concentrations. One of \code{"mg/L"}, \code{"mmol/L"}, or \code{"meq/L"}. Default is \code{"mg/L"}.}

\item{tds_method}{Character. Method to compute total dissolved solids (TDS). \code{"major"} uses only Na, Ca, Cl, HCO3; \code{"major_si"} includes additional major ions (e.g. Mg, SO4); \code{"all_ions"} uses all columns. Default is \code{"major"}.}

\item{show_guide}{Logical. If \code{TRUE}, displays dashed reference lines approximating domain boundaries and transitions
within the Gibbs diagram (e.g., evaporation dominance, rock weathering, precipitation influence). Default is \code{TRUE}.}

\item{show_text}{Logical. If TRUE, labels for the dominance zones are displayed on the plot.}

\item{group}{Optional character string. Column name in \code{data} used to group points. If \code{NULL}, all points are styled the same. Default is \code{NULL}.}

\item{group_custom}{Logical. If \code{TRUE}, allows custom aesthetics (color, fill, shape, size) per group. Must supply vectors of appropriate length. If \code{FALSE}, all aesthetics must be scalars. Default is \code{FALSE}.}

\item{base_size}{Numeric. Base font size for the plot. Default is \code{12}.}

\item{shape}{Integer or vector of integers. Shape(s) for plotting points (ggplot2 standard). Default is \code{21}.}

\item{color}{Character. Outline color of points. Default is \code{"black"}.}

\item{fill}{Character. Fill color(s) for points. Default is \code{"grey40"}.}

\item{size}{Numeric. Size of points. Default is \code{2}.}

\item{plot_title}{Optional character string. Plot title. Default is \code{NULL}.}

\item{label}{Optional column name in \code{data} used to label points. Default is \code{NULL} (no labels).}

\item{label_size}{Numeric. Size of the label text. Default is \code{3}.}

\item{label_nudge_x}{Numeric. Horizontal offset of labels relative to points. Default is \code{0}.}

\item{label_nudge_y}{Numeric. Vertical offset of labels relative to points. Default is \code{0}.}

\item{legend.position}{Position of the legend. Accepts \code{"top"}, \code{"bottom"}, \code{"left"}, \code{"right"}, \code{"none"}, or a numeric vector of length 2 for relative coordinates, e.g., \code{c(1, 0.5)}. Default is \code{"bottom"}.}

\item{legend.title}{A character string for the title of the legend. If \code{NULL}, no legend title is displayed. Default is \code{NULL}.}

\item{bg_color}{Character. Background color of the plot area and panel. Accepts valid color names or hex codes (e.g., "red", "#FFFFFF"). Default is "#FFFFFF".}
}
\value{
A \code{ggplot} object showing the Gibbs diagram.
}
\description{
Generates a Gibbs diagram from hydrochemical data.
Input concentrations may be
provided in \code{"mg/L"}, \code{"mmol/L"}, or \code{"meq/L"} and are internally converted
to \code{"meq/L"} prior to calculation.
Optionally groups data points and allows manual customization of plotting aesthetics.
}
\examples{
data("hc_data", package = "hydrochem")

# Basic Gibbs diagram
plot_gibbs(hc_data)

# With grouping
plot_gibbs(hc_data, group = "type")

# With custom fill and shape
plot_gibbs(
  hc_data,
  group = "type",
  group_custom = TRUE,
  fill = c("skyblue", "tomato", "green"),
  shape = c(21, 22, 23)
)

# With all ions included in TDS computation
plot_gibbs(hc_data, tds_method = "all_ions")

}
