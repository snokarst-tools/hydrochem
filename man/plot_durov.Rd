% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_durov.R
\name{plot_durov}
\alias{plot_durov}
\title{Plot Durov Diagram from Hydrochemical Data}
\usage{
plot_durov(
  data,
  base_unit = c("mg/L", "mmol/L", "meq/L"),
  top_ions = c("Cl", "SO4", "HCO3"),
  left_ions = c("Mg", "Na", "Ca"),
  tds_method = c("major", "major_si", "all_ions"),
  group = NULL,
  group_custom = FALSE,
  grid_lines = TRUE,
  plot_pH = TRUE,
  plot_TDS = TRUE,
  tds_log = FALSE,
  axis_title_size = 3.5,
  axis_tick_label_size = 3,
  shape = 21,
  color = "black",
  fill = "grey40",
  size = 2,
  plot_title = NULL,
  label = NULL,
  label_size = 3,
  label_nudge_x = 0,
  label_nudge_y = 0,
  legend.position = "bottom",
  legend.title = NULL,
  bg_color = "#FFFFFF"
)
}
\arguments{
\item{data}{A \code{data.frame} or \code{data.table} with columns named after ions (e.g., \code{"Ca"}, \code{"Cl"}, \code{"SO4"}).}

\item{base_unit}{Unit of the input ion concentrations. One of \code{"mg/L"}, \code{"mmol/L"}, or \code{"meq/L"}. Default is \code{"mg/L"}.}

\item{top_ions}{Character vector (length 3) of anions used on the top triangle,
default \code{c("Cl","SO4","HCO3")}. If \code{"CO3"} exists, it is combined with \code{"HCO3"}
unless \code{"HCO3"} is absent.}

\item{left_ions}{Character vector (length 3) of cations used on the left triangle,
default \code{c("Mg","Na","Ca")}. If \code{"K"} is present, it is typically merged with \code{"Na"}.}

\item{tds_method}{TDS estimation for the Durov square: \code{"major"} (sum of major ions),
\code{"major_si"} (major ions incl. silica if present), or \code{"all_ions"} (sum of all available ions).}

\item{group}{Optional column name in \code{data} used to group points. If \code{NULL}, a single style is used.}

\item{group_custom}{Logical. If \code{TRUE}, enables manual aesthetics per group defined in \code{group}.
In that case, at least one of \code{color}, \code{fill}, \code{shape}, or \code{size} must be a vector of
length equal to the number of groups. Missing or wrongly sized vectors are filled with
defaults (\code{"black"}, \code{"transparent"}, \code{21}, \code{2}). If \code{FALSE} (default), these aesthetics
must be length 1.}

\item{grid_lines}{Logical. Draw dashed grid lines on the ternaries and Durov square. Default \code{TRUE}.}

\item{plot_pH}{Logical. If \code{TRUE}, adds the optional pH scale plot below the main
Durov diagram. Default \code{TRUE}.}

\item{plot_TDS}{Logical. If \code{TRUE}, adds the optional TDS scale plot to the right
of the main Durov diagram. Default \code{TRUE}.}

\item{tds_log}{Logical. If \code{TRUE}, the TDS axis on the right panel is plotted
on a base-10 logarithmic scale. Default \code{FALSE}.}

\item{axis_title_size}{Numeric. Size for ion-group labels. Default \code{3.5}.}

\item{axis_tick_label_size}{Numeric. Size for tick labels. Default \code{3}.}

\item{shape}{Integer or integer vector of point shapes (ggplot2 standards). Default \code{21}.}

\item{color}{Character. Point outline color. Default \code{"black"}.}

\item{fill}{Character. Point fill color. Default \code{"grey40"}.}

\item{size}{Numeric or numeric vector. Point size(s). Default \code{2}.}

\item{plot_title}{Optional character string. Title of the plot. Default is NULL (no title displayed).}

\item{label}{Optional column name in \code{data} used to label points. Default is \code{NULL} (no labels).}

\item{label_size}{Numeric. Size of the label text. Default is \code{3}.}

\item{label_nudge_x}{Numeric. Horizontal offset of labels relative to points. Default is \code{0}.}

\item{label_nudge_y}{Numeric. Vertical offset of labels relative to points. Default is \code{0}.}

\item{legend.position}{Position of the legend. Accepts \code{"top"}, \code{"bottom"}, \code{"left"}, \code{"right"}, \code{"none"}, or a numeric vector of length 2 for relative coordinates, e.g., \code{c(1, 0.5)}. Default is \code{"bottom"}.}

\item{legend.title}{A character string for the title of the legend. If \code{NULL}, no legend title is displayed. Default is \code{NULL}.}

\item{bg_color}{Character. Background color of the plot area and panel. Accepts valid color names or hex codes (e.g., "red", "#FFFFFF"). Default is "#FFFFFF".}
}
\value{
A \code{ggplot} object showing the Durov diagram.
}
\description{
Generates a Durov diagram from hydrochemical data.
Input concentrations may be
provided in \code{"mg/L"}, \code{"mmol/L"}, or \code{"meq/L"} and are internally converted
to \code{"meq/L"} prior to calculation.
If carbonate (\code{CO3}) is also provided, its contribution is added
to bicarbonate after conversion to milliequivalents (\code{meq/L}). If \code{CO3} is absent, alkalinity is considered to be represented solely by bicarbonate (\code{HCO3}).
Optionally groups data points and allows manual customization of plotting aesthetics.
}
\examples{
data("data_qc", package = "hydrochem")

# Replace below detection limit values
data <- replace_bdl(data_qc)

# Basic Durov plot
plot_durov(data)

# Grouped Durov plot
plot_durov(data, group = "Cluster")

# Custom aesthetics per group
plot_durov(
  data,
  group = "Hydro_cond",
  group_custom = TRUE,
  fill = c("steelblue", "tomato", "darkgreen", "yellow"),
  color = c("black", "black", "black", "black"),
  shape = c(21, 22, 24, 25),
  size  = c(2.5, 3, 3.5, 4)
)

}
