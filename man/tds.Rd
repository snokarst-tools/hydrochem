% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tds.R
\name{tds}
\alias{tds}
\title{Calculate Total Dissolved Solids (TDS)}
\usage{
tds(
  data,
  base_unit = c("mg/L", "mmol/L", "meq/L"),
  method = c("major", "major_si", "all_ions")
)
}
\arguments{
\item{data}{A \code{data.frame} or \code{data.table} with columns named after ions (e.g., \code{"Ca"}, \code{"Cl"}, \code{"SO4"}).}

\item{base_unit}{Unit of the input ion concentrations. One of \code{"mg/L"}, \code{"mmol/L"}, or \code{"meq/L"}. Default is \code{"mg/L"}.}

\item{method}{Character string indicating the calculation method. One of:
\itemize{
\item \code{"major"} (default): uses major ions (Ca, Mg, Na, K, HCO3, CO3, Cl, SO4, NO3),
\item \code{"major_si"}: adds SiO2 to the major ions (can be calculated with Si if only available),
\item \code{"all_ions"}: uses all available ions in the dataset and SiO2.
}}
}
\value{
A numeric vector of TDS values, one per row of \code{data}.
}
\description{
Computes the total dissolved solids (TDS) of a hydrochemical dataset by summing the concentrations
of selected ions in mg/L. Input concentrations may be
provided in \code{"mg/L"}, \code{"mmol/L"}, or \code{"meq/L"} and are internally converted
to \code{"mg/L"} prior to calculation.
}
\examples{
data("hc_data", package = "hydrochem")
tds(hc_data, base_unit = "mg/L")

}
