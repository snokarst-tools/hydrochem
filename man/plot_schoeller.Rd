% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_schoeller.R
\name{plot_schoeller}
\alias{plot_schoeller}
\title{Plot Schoeller Diagrams from Hydrochemical Data}
\usage{
plot_schoeller(
  data,
  base_unit = c("mg/L", "mmol/L", "meq/L"),
  include_NO3 = FALSE,
  mg_guide = c("full", "simple", "none"),
  group = NULL,
  group_custom = FALSE,
  base_size = 12,
  bold_label = FALSE,
  line_color = "black",
  line_width = 1,
  plot_title = NULL,
  legend.position = "bottom",
  legend.title = NULL,
  bg_color = "#FFFFFF"
)
}
\arguments{
\item{data}{A \code{data.frame} or \code{data.table} with columns named after ions (e.g., \code{"Ca"}, \code{"Cl"}, \code{"SO4"}).}

\item{base_unit}{Unit of the input ion concentrations. One of \code{"mg/L"}, \code{"mmol/L"}, or \code{"meq/L"}. Default is \code{"mg/L"}.}

\item{include_NO3}{Logical. If \code{TRUE}, nitrate (\code{NO3}) is included. The \code{NO3} column must be present in the input data. Default is \code{FALSE}.}

\item{mg_guide}{Character. Display mode for mg/L horizontal guide lines in the Schoeller diagram. \code{"full"} shows lines with labels, \code{"simple"} shows only dashed lines, and \code{"none"} hides them completely. Default is \code{"full"}.
Major ticks (0.01, 0.1, 1, 10) are emphasized. Labels appear to the left for \code{Na} and \code{HCO3}, to the right for \code{K} and \code{CO3} (if present).}

\item{group}{Optional character string. Name of the column used for coloring groups. Default is \code{NULL}.}

\item{group_custom}{Logical. If \code{TRUE}, enables manual aesthetics for each group defined in \code{group}. Requires a vector \code{color} of the same length as number of groups. Default is \code{FALSE}.}

\item{base_size}{Numeric. Base font size for the plot. Default is \code{12}.}

\item{bold_label}{Logical. If \code{TRUE}, displays axis labels in bold. Default is \code{FALSE}.}

\item{line_color}{Character or vector. Line color(s). If \code{group_custom = TRUE}, must be a vector. Default is \code{"black"}.}

\item{line_width}{Numeric. Line width. Default is \code{1}.}

\item{plot_title}{Optional character string. Title of the plot. Default is NULL (no title displayed).}

\item{legend.position}{Position of the legend. Accepts \code{"top"}, \code{"bottom"}, \code{"left"}, \code{"right"}, \code{"none"}, or a numeric vector of length 2 for relative coordinates, e.g., \code{c(1, 0.5)}. Default is \code{"bottom"}.}

\item{legend.title}{A character string for the title of the legend. If \code{NULL}, no legend title is displayed. Default is \code{NULL}.}

\item{bg_color}{Character. Background color of the plot area and panel. Accepts valid color names or hex codes (e.g., "red", "#FFFFFF"). Default is "#FFFFFF".}
}
\value{
A \code{ggplot} object representing the Schoeller diagram.
}
\description{
Generates Schoeller diagrams from hydrochemical data.
Input concentrations may be
provided in \code{"mg/L"}, \code{"mmol/L"}, or \code{"meq/L"} and are internally converted
to \code{"meq/L"} prior to calculation.
Optionally groups data points and allows manual customization of plotting aesthetics.
}
\examples{
data("hc_data", package = "hydrochem")
hc_data <- hc_data[1:20, ] # keep first 20 rows

# Basic Schoeller plot
plot_schoeller(hc_data)

# Remove legend
plot_schoeller(hc_data, legend.position = "none")

# Schoeller plot with mg/L guides hidden
plot_schoeller(hc_data, mg_guide = "none")

# Specify a group by unique id
plot_schoeller(hc_data, group = "id")

# Schoeller plot with group coloring
plot_schoeller(hc_data, group = "type")

# Schoeller plot with custom group colors
plot_schoeller(
  hc_data,
  group = "type",
  group_custom = TRUE,
  line_color = c("red", "blue")
)

# Schoeller plot with NO3 included
plot_schoeller(hc_data, include_NO3 = TRUE)

}
