% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ionic_balance.R
\name{ionic_balance}
\alias{ionic_balance}
\title{Calculate Ionic Balance Error (\%)}
\usage{
ionic_balance(
  data,
  base_unit = c("mg/L", "mmol/L", "meq/L"),
  method = c("major", "major_minor")
)
}
\arguments{
\item{data}{A \code{data.frame} or \code{data.table} with numeric columns named after ions (e.g., \code{"Ca"}, \code{"Cl"}, \code{"SO4"}).}

\item{base_unit}{Unit of the input ion concentrations. One of \code{"mg/L"}, \code{"mmol/L"}, or \code{"meq/L"}. Default is \code{"mg/L"}.}

\item{method}{A character string specifying which ions to include. \code{"major"} uses only major ions;
\code{"major_minor"} includes both major and minor ions.}
}
\value{
A numeric vector of ionic balance error (in \%), one per row of \code{data}.
}
\description{
Computes the ionic balance error for each row of a hydrochemical dataset.
}
\details{
The ionic balance error is calculated in percent:
\deqn{IB = ((T_c - T_a) / (T_c + T_a)) * 100}
where \eqn{T_c} is the total concentration of cations in meq/L,
and \eqn{T_a} is the total concentration of anions in meq/L.
}
\examples{
data("hc_data", package = "hydrochem")
hc_data$ib <- ionic_balance(hc_data,
                            base_unit = "mg/L")

}
