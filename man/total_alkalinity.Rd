% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/total_alkalinity.R
\name{total_alkalinity}
\alias{total_alkalinity}
\title{Calculate Total Alkalinity}
\usage{
total_alkalinity(
  data,
  base_unit = c("mg/L", "mmol/L", "meq/L"),
  output = c("mg/L_CaCO3", "meq/L")
)
}
\arguments{
\item{data}{A \code{data.frame} or \code{data.table} containing the required columns
\code{"HCO3"} and \code{"pH"}.}

\item{base_unit}{Unit of the input ion concentrations. One of \code{"mg/L"},
\code{"mmol/L"}, or \code{"meq/L"}. Default is \code{"mg/L"}.}

\item{output}{Unit of the returned hardness.
One of:
\itemize{
\item \code{"mg/L_CaCO3"} — milligrams per liter as CaCO3 (default)
\item \code{"meq/L"} — milliequivalents per liter
}}
}
\value{
A numeric vector of Total Alkalinity values (in mg/L as CaCO3), one per row of
\code{data}.
}
\description{
Computes total alkalinity from hydrochemical data using bicarbonate/carbonate
concentrations and pH. Input concentrations may be provided in \code{"mg/L"},
\code{"mmol/L"}, or \code{"meq/L"} and are internally converted to
\code{"meq/L"} prior to calculation.
}
\details{
The function checks that both required variables (\code{HCO3}, \code{pH}) are present in the input dataset and throws an error if either are
missing. If carbonate (\code{CO3}) is also provided, its contribution is added
to bicarbonate after conversion to milliequivalents (\code{meq/L}). If \code{CO3} is absent, alkalinity is considered to be represented solely by bicarbonate (\code{HCO3}).
}
\examples{
data("data_qc", package = "hydrochem")
total_alkalinity(data_qc, base_unit = "mg/L")

}
