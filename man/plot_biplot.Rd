% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_biplot.R
\name{plot_biplot}
\alias{plot_biplot}
\title{Plot Biplots from Hydrochemical Data}
\usage{
plot_biplot(
  data,
  pairs,
  base_unit = c("mg/L", "mmol/L", "meq/L"),
  to_unit = c("meq/L", "mmol/L", "mg/L"),
  ncol = NULL,
  group = NULL,
  group_custom = FALSE,
  grid_lines = TRUE,
  slope = NA,
  intercept = NA,
  slope_color = "black",
  xmin = NA,
  xmax = NA,
  ymin = NA,
  ymax = NA,
  log_x = FALSE,
  log_y = FALSE,
  shape = 21,
  color = "black",
  fill = "grey40",
  size = 2,
  label = NULL,
  label_size = 3,
  label_nudge_x = 0,
  label_nudge_y = 0,
  legend.position = "bottom",
  legend.title = NULL,
  bg_color = "#FFFFFF"
)
}
\arguments{
\item{data}{A \code{data.frame} or \code{data.table} with columns named after ions (e.g., \code{"Ca"}, \code{"Cl"}, \code{"SO4"}).}

\item{pairs}{Character vector of biplot definitions using the \code{"y ~ x"} syntax.
Each element defines one panel. Arithmetic expressions are allowed on either side,
e.g. \code{"Ca + Mg ~ Cl"}, \code{"Na * K ~ Cl"}, \code{"Na - K ~ Cl"}, or \code{"SO4 / Cl ~ NO3"}. Whitespace is ignored.}

\item{base_unit}{Unit of the input ion concentrations. One of \code{"mg/L"}, \code{"mmol/L"}, or \code{"meq/L"}. Default is \code{"mg/L"}.}

\item{to_unit}{Unit of the output ion concentrations. One of \code{"meq/L"}, \code{"mmol/L"}, or \code{"mg/L"}. Default is \code{"meq/L"}.}

\item{ncol}{Integer. Number of columns for the cowplot grid layout. Default is \code{NULL} (automatic layout).}

\item{group}{Optional column name in \code{data} used to group points. If \code{NULL}, a single style is used.}

\item{group_custom}{Logical. If \code{TRUE}, enables manual aesthetics per group defined in \code{group}.
In that case, at least one of \code{color}, \code{fill}, \code{shape}, or \code{size} must be a vector of
length equal to the number of groups. Missing or wrongly sized vectors are filled with
defaults (\code{"black"}, \code{"transparent"}, \code{21}, \code{2}). If \code{FALSE} (default), these aesthetics
must be length 1.}

\item{grid_lines}{Logical. Draw grid lines on panels. Default \code{TRUE}.}

\item{slope}{Numeric or numeric vector. Slope of a reference line drawn on the biplots.
If length 1, the same slope is applied to all panels. If a vector, it must have
the same length as \code{pairs}, with \code{NA} indicating that no line is drawn for a
given panel. Must be provided together with \code{intercept}. Default is \code{NULL}
(no reference line). Horizontal and vertical lines can be displayed using 0 and Inf, respectively.}

\item{intercept}{Numeric or numeric vector. Intercept of a reference line drawn on
the biplots. If length 1, the same intercept is applied to all panels. If a vector,
it must have the same length as \code{pairs}, with \code{NA} indicating that no line is drawn
for a given panel. Must be provided together with \code{slope}. Default is \code{NULL}
(no reference line).}

\item{slope_color}{Character. Color of the reference line. Default is \code{"black"}.}

\item{xmin}{Numeric or numeric vector. Minimum value of the x-axis. If length 1, the same limit is applied to all biplots. If a vector, it must have the same length as \code{pairs}, with \code{NA} indicating that the limit is derived from the data for a given panel. Default is \code{NA}.}

\item{xmax}{Numeric or numeric vector. Maximum value of the x-axis. If length 1, the same limit is applied to all biplots. If a vector, it must have the same length as \code{pairs}, with \code{NA} indicating that the limit is derived from the data for a given panel. Default is \code{NA}.}

\item{ymin}{Numeric or numeric vector. Minimum value of the y-axis. If length 1, the same limit is applied to all biplots. If a vector, it must have the same length as \code{pairs}, with \code{NA} indicating that the limit is derived from the data for a given panel. Default is \code{NA}.}

\item{ymax}{Numeric or numeric vector. Maximum value of the y-axis. If length 1, the same limit is applied to all biplots. If a vector, it must have the same length as \code{pairs}, with \code{NA} indicating that the limit is derived from the data for a given panel. Default is \code{NA}.}

\item{log_x}{Logical or logical vector. If \code{TRUE}, the x-axis is displayed on a
base-10 logarithmic scale. If length 1, the same setting is applied to all
biplots. If a vector, it must have the same length as \code{pairs}, with \code{FALSE}
indicating a linear scale for a given panel. Default is \code{FALSE}.}

\item{log_y}{Logical or logical vector. If \code{TRUE}, the y-axis is displayed on a
base-10 logarithmic scale. If length 1, the same setting is applied to all
biplots. If a vector, it must have the same length as \code{pairs}, with \code{FALSE}
indicating a linear scale for a given panel. Default is \code{FALSE}.}

\item{shape}{Integer or integer vector of point shapes (ggplot2 standards). Default \code{21}.}

\item{color}{Character. Point outline color. Default \code{"black"}.}

\item{fill}{Character. Point fill color. Default \code{"grey40"}.}

\item{size}{Numeric or numeric vector. Point size(s). Default \code{2}.}

\item{label}{Optional column name in \code{data} used to label points. Default is \code{NULL} (no labels).}

\item{label_size}{Numeric. Size of the label text. Default is \code{3}.}

\item{label_nudge_x}{Numeric. Horizontal offset of labels relative to points. Default is \code{0}.}

\item{label_nudge_y}{Numeric. Vertical offset of labels relative to points. Default is \code{0}.}

\item{legend.position}{Position of the legend. Accepts \code{"top"}, \code{"bottom"}, \code{"left"}, \code{"right"}, \code{"none"}, or a numeric vector of length 2 for relative coordinates, e.g., \code{c(1, 0.5)}. Default is \code{"bottom"}.}

\item{legend.title}{A character string for the title of the legend. If \code{NULL}, no legend title is displayed. Default is \code{NULL}.}

\item{bg_color}{Character. Background color of the plot area and panel. Accepts valid color names or hex codes (e.g., "red", "#FFFFFF"). Default is "#FFFFFF".}
}
\value{
A combined cowplot object containing the biplots (and legend if grouping is used).
}
\description{
Generates simple XY biplots from hydrochemical data using user-defined variable
pairs. Pairs are provided as character strings using the \code{"y ~ x"} syntax and
#' can include arithmetic expressions (e.g., \code{"Ca + Mg ~ Cl"} or \code{"Na / Cl ~ SO4"}). Input concentrations
may be provided in \code{"mg/L"}, \code{"mmol/L"}, or \code{"meq/L"} and are internally converted
to \code{"meq/L"} prior to calculation.
Optionally groups data points and allows manual customization of plotting aesthetics.
}
\examples{
data("data_qc", package = "hydrochem")

# Replace below detection limit values
data <- replace_bdl(data_qc)

# Basic biplots
plot_biplot(
  data,
  pairs = c("Ca ~ Mg", "SO4 ~ Cl", "Cl + SO4 ~ NO3_N")
)

# Grouped biplots
plot_biplot(
  data,
  pairs = c("Ca ~ HCO3", "Na ~ Cl"),
  group = "Cluster"
)

# Custom aesthetics per group
plot_biplot(
  data,
  pairs = c("Ca ~ HCO3", "Na + K ~ Cl"),
  group = "Hydro_cond",
  group_custom = TRUE,
  fill = c("steelblue", "tomato", "darkgreen", "yellow"),
  color = c("black", "black", "black", "black"),
  shape = c(21, 22, 24, 25),
  size  = c(2.5, 3, 3.5, 4),
  ncol = 2
)

# Same reference line (1:1) on all biplots
plot_biplot(
  data,
  pairs = c("Ca ~ Ca", "Na ~ Na"),
  slope = 1,
  intercept = 0
)

# Reference line on a single panel only
plot_biplot(
  data,
  pairs = c("Ca ~ Ca", "Ca + Mg ~ HCO3", "Cl ~ Na"),
  slope     = c(NA, NA, 1),
  intercept = c(NA, NA, 0)
)

# Log scale on x-axis for all biplots
plot_biplot(
  data,
  pairs = c("Ca ~ Mg", "Cl ~ Na"),
  log_x = TRUE
)

# Log scale on selected panels only
plot_biplot(
  data,
  pairs = c("Ca ~ Mg", "Ca + Mg ~ HCO3", "Cl ~ Na"),
  log_x = c(FALSE, TRUE, FALSE),
  log_y = c(FALSE, FALSE, TRUE)
)

}
